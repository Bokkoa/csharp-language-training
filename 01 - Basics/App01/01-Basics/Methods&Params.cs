//// 1 . The user enter the number that represent the number of how many times a random value is going to be generated
//// 2. Inside the loop the random number needs to be generated with a value from 1 to 1000
//// 3. Print inside the loop this random number


//String message = "Hello number: ";
//PrintRandomNumber(20, message);

//// WITH DEFAULT VALUE
//PrintRandomNumber(10);

//// BY PARAM NAMES
//PrintRandomNumber(message: "~~~~~~", rangeNumber: 5);


//void PrintRandomNumber(int rangeNumber, string message = "@@@@@@@@@@@@@")
//{

//    var random = new Random();

//    for (int i = 0; i <= rangeNumber; i++)
//    {
//        var randomNumber = random.Next(1, 100);
//        Console.Write($"{message} {randomNumber} \n ");
//    }
//}
